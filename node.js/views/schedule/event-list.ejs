<% for (var e in eventList) {%>
<dd <%= (typeof className != "undefined" && className ? "class='" + className + (e == eventList.length - 1 ? " lastAdded" : "") + "'" : "") %>><%- partial('schedule/event-desc.ejs', {event: eventList[e], interestMarker:true}) %></dd>
<% } %>
